# for local testing (cpu)
torchvision==0.11.3
torch==1.10.2

# for metrics reporting/plotting
azureml-mlflow==1.40.0
mlflow==2.2.1
matplotlib==3.5.1

# to run the notebooks locally
ipykernel==6.13.0
mlflow-skinny>=3.10.0 # not directly required, pinned by Snyk to avoid a vulnerability
paramiko>=5.0.0 # not directly required, pinned by Snyk to avoid a vulnerability
urllib3>=2.7.0 # not directly required, pinned by Snyk to avoid a vulnerability
